version: '3.8'

services:
  main_db:
    image: postgres:latest
    env_file:
      - .env.sample
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_DB: "main"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "123handrushka123"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"


  redis:
    image: redis:latest
    env_file:
      - .env.sample
    ports:
      - "6379:6379"

  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/code
    ports:
      - "8010:8000"
    environment:
      - POSTGRES_DB_HOST=database-1.c1cquyec4scf.eu-north-1.rds.amazonaws.com
      - REDIS_DB_HOST=redis
    depends_on:
      - main_db
      - redis
    env_file:
      - .env.sample

  celery_worker:
    build:
      context: .
      dockerfile: Dockerfile
    command: celery -A app.celery:celery worker --loglevel=info
    volumes:
      - .:/code
    environment:
      - POSTGRES_DB_HOST=database-1.c1cquyec4scf.eu-north-1.rds.amazonaws.com
      - REDIS_DB_HOST=redis
    depends_on:
      - main_db
      - redis
    env_file:
      - .env.sample

  celery_beat:
    build:
      context: .
      dockerfile: Dockerfile
    command: celery -A app.celery:celery beat --loglevel=info
    volumes:
      - .:/code
    environment:
      - POSTGRES_DB_HOST=database-1.c1cquyec4scf.eu-north-1.rds.amazonaws.com
      - REDIS_DB_HOST=redis
    depends_on:
      - redis
      - main_db
      - celery_worker
    env_file:
      - .env.sample

  flower:
    build:
      context: .
      dockerfile: Dockerfile
    command: celery -A app.celery:celery flower --port=5555
    volumes:
      - .:/code
    environment:
      - POSTGRES_DB_HOST=database-1.c1cquyec4scf.eu-north-1.rds.amazonaws.com
      - REDIS_DB_HOST=redis
    depends_on:
      - redis
      - main_db
      - celery_worker
    env_file:
      - .env.sample
    ports:
      - "5555:5555"

volumes:
  postgres_data:
